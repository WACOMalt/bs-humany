# docker compose up -d --build   (or: podman compose up -d --build)
services:
  studio:
    build: .
    image: bs-humany-studio
    container_name: bs-humany
    ports:
      - "8080:80"
    restart: unless-stopped
