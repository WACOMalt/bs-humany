/**
 * `@bs-humany/muscle-volume` -- Tier V, the muscle you can see.
 *
 * A line of action cannot show a muscle bulge. This sweeps a cross-section along the solved path
 * and gives the belly a girth that follows from the muscle's own volume, so shortening thickens
 * it. Display only: Tier V writes to the render channel and to nothing else (M-ADR-004).
 */

export * from './sweep.js';
